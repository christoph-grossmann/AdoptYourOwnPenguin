\begin{block}{SSH login}%
	\begin{tabular}{p{\bashcolumnsize}p{\desccolumnsize}}
		\bcom{ssh [user]@[host]}{Connect to \texttt{[host]} as \texttt{[user]} using ssh}\\
		\bcom{ssh [host]}{Connect to \textit{[host]} via port 22}\\
		\bcom{ssh -p [port] [user]@[host]}{Connect to \texttt{[host]} as \texttt{[user]} using a non-default \textit{[port]}}\\
	\end{tabular}
\end{block}
